version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://localhost:8080/api/v1
      platform: linux/amd64
    platform: linux/amd64
    ports:
      - "80:80"

